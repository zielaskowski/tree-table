---
title: "treetable"
output: 
  html_document:
    self_contained: FALSE
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
library(DT)
library(dplyr)
library(magrittr)
library(purrr)
library(data.tree)
library(htmlwidgets)
library(rstudioapi)

setwd(dirname(getActiveDocumentContext()$path))

source(file="../R/TT.r")

load(file="../data/col_order.rda")
load(file="../data/org.rda")
load(file="../R/sysdata.rda")

colnames <- factor(c("org",org$attributesAll),
                   levels =  col_order)
```

```{r echo=FALSE}
# std DT example
# dates
treetable(data.frame(
  date = seq(as.Date("2015-01-01"), by = "day", length.out = 5), x = 1:5
))
```


<script src="../data-raw/kolorWheel-min.js"></script>
<script src="../data-raw/TT_lev.js"></script>


```{r echo=FALSE}
#treetable(org, colnames = col_order) %>% formatRound(columns = c(2:11), digits = 0)
res <- treetable(org, color="#FFFFFF", colnames=colnames)

# remove appended js with files so easier debugging
res$append <- ""

res
```


# ```{r}
# #minimum example
# treetable(org)
# ```





